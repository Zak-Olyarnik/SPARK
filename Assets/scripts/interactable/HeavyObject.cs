using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class HeavyObject : MonoBehaviour {
	/* 
	 * this script is applied to objects to signify them as "Heavy":
	 * -Beta cannot carry them
	 * -they can be used to activate pressure plates
	 */
}
